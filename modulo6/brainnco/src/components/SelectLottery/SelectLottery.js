import React from 'react';


const SelectLottery = (props) => {

    return (
        <div>
            <select>
                {props.name}
            </select>
        </div>
    );
};

export default SelectLottery;